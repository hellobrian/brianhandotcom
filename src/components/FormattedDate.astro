---
interface Props {
  date: Date;
  lastUpdated?: string;
}

const { date, lastUpdated } = Astro.props;
---

<time datetime={date.toISOString()} title={lastUpdated}>
  {
    date.toLocaleDateString("en-us", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
