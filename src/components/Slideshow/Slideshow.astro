---
import SlideshowComponent from "./Slideshow";

type Props = {
  columns?: number;
  images: string[];
};

const { columns = 3, images } = Astro.props as Props;
---

<SlideshowComponent client:load columns={columns} images={images} />
