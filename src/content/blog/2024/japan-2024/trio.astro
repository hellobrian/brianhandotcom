<div class="trio">
  <slot />
</div>

<style>
  @keyframes zoomIn {
    0% {
      transform: scale(1);
    }
    100% {
      transform: scale(1.2);
    }
  }

  :global(.trio) {
    grid-column: 2 / span 3 !important;
    max-inline-size: 1280px;
    inline-size: 100%;
    block-size: 100%;
  }

  :global(.trio > *:nth-child(2)) {
    block-size: 100%;
    object-fit: cover;
  }

  .trio {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 1rem;
  }

  @media screen and (width < 900px) {
    .trio {
      grid-column: 1 / span 1 !important;
      display: flex;
      overflow: scroll;
    }
  }
</style>
